#include <stdio.h>
int main() {
    float base, height;
    printf("Enter Base: ");
    scanf("%f", &base);
    printf("Enter Height: ");
    scanf("%f", &height);
    printf("Area = %f\n", (base * height) / 2);
    return 0;
}


